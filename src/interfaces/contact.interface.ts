export interface ICreatContact {
  name: string;
  email: string;
  number: number;
  userid: string;
}

export interface IContact {
  id: string;
  email: string;
  number: number;
  userid: string;
}
